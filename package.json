{
  "name": "color-balls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "size": "npm run build && node scripts/size.mjs",
    "build:portfolio": "npm run build && node scripts/build-portfolio.mjs",
    "preview:portfolio": "npm run build:portfolio && npx --yes serve portfolio -p 4174"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "pixi.js": "^8.19.0"
  }
}
