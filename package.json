{
  "name": "color-balls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "size": "npm run build && node -e \"const fs=require('fs');const zlib=require('zlib');const buf=fs.readFileSync('dist/index.html');console.log('Gzipped size:', zlib.gzipSync(buf).length, 'bytes')\""
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "pixi.js": "^8.19.0"
  }
}
